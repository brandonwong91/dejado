import ProfileViewPage from '@/features/profile/components/profile-view-page';

export const metadata = {
  title: 'Profile | Dejado'
};

export default async function Page() {
  return <ProfileViewPage />;
}
